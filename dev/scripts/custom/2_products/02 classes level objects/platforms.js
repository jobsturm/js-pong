class Platform extends Shape {
    constructor (width, height, color, weight) {
        super(width, height, color, weight);
        this.width    = width;
        this.height   = height;
        this.color    = color;
    }
}
